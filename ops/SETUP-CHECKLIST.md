# What I need from you (≈10 min, one-time)

I cannot own accounts or money. Do these, paste the results into `ops/.env` (gitignored), and I'll take it from there.

1. **Gumroad** — create account at gumroad.com (free). Settings → Advanced → Applications → create app → copy **access token** → `GUMROAD_TOKEN=...`
2. **Payout** — connect your bank/PayPal in Gumroad payouts so revenue actually lands somewhere.
3. **GitHub** — a repo I can push to (or tell me to use a local-only site); enable Pages. `GITHUB_REPO=owner/name`
4. **Domain (optional, ~$12)** — only planned spend. Approve or reject: [ ] approve  [ ] skip (use *.gumroad.com + *.github.io for now)

That's it. Everything else — products, copy, pages, listings, reports — I do.
